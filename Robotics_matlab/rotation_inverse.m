function Rt=rotation_inverse(R) 
%Function to calculate the Inverse of a Rotation Matrix,
%   Rt = rotation_inverse(R)
%   Inputs:   R - Rotation Matrix   
%   Output:   SO[3] inverse of R
Rt = transpose(R);
end